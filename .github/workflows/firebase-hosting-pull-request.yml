# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools
# Modified for troubleshooting direct Firebase CLI deploy with debug output

name: Deploy to Firebase Hosting on PR
on: pull_request
permissions:
  checks: write
  contents: read
  pull-requests: write
jobs:
  build_and_preview:
    if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # --- START Troubleshooting Step: Attempt direct Firebase CLI deploy ---
      # This step is temporary to get detailed error messages from the Firebase CLI.
      # It installs the Firebase CLI and then tries to deploy Hosting,
      # authenticating using the service account secret.
      - name: Deploy Hosting (for troubleshooting and detailed error messages)
        run: |
          npm install -g firebase-tools # Install Firebase CLI globally
          
          # Create a temporary file for the service account key
          echo "${{ secrets.FIREBASE_SERVICE_ACCOUNT_UN_HOUSE_GARAGE_MGT_SYSTEM_26 }}" > ./firebase-service-account.json
          
          # Set the GOOGLE_APPLICATION_CREDENTIALS environment variable
          # This tells the Firebase CLI to use this file for authentication
          export GOOGLE_APPLICATION_CREDENTIALS=$(pwd)/firebase-service-account.json
          
          # Execute the Firebase deploy command with verbose debug logging
          # This is the command that should give us the precise reason for failure
          firebase deploy --only hosting --project un-house-garage-mgt-system-26 --debug
        working-directory: . # Run this command from the repository root
      # --- END Troubleshooting Step ---

      # The original 'FirebaseExtended/action-hosting-deploy@v0' step is
      # commented out for this troubleshooting run to focus on the raw CLI output.
      # We will uncomment it later once the core issue is identified.
      # - uses: FirebaseExtended/action-hosting-deploy@v0
      #   with:
      #     repoToken: '${{ secrets.GITHUB_TOKEN }}'
      #     firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_UN_HOUSE_GARAGE_MGT_SYSTEM_26 }}'
      #     projectId: un-house-garage-mgt-system-26
